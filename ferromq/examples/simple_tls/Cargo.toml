[package]
name = "simple_tls"
version = "0.1.0"
edition.workspace = true

[dependencies]
ferromq = { workspace = true, features = ["tls"] }
tokio = { version = "1", features = ["full"] }
simple_logger = { workspace = true }
log = "0.4"

